<nav class="navbar navbar-expand-lg navbar-dark bg-info">
  <a class="navbar-brand p-3" href="/user/games/index"><%= image_tag('logo.png', style:'width:120px;height:70px;object-fit:cover;') %></a>
  
  
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
</button>
    
  
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    

<% unless current_user.email == current_user.guest_email %>
  <%= link_to user_user_path(current_user.id), class: "nav-link text-dark" do %>
  <i class="fas fa-house-user"></i>
    <span>マイページ</span>
  <% end %>
<% end %>

  <%= link_to user_games_index_path, class: "nav-link text-dark" do %>
  <i class="fas fa-gamepad"></i>
    <span>ゲーム一覧</span>
  <% end %>
<%
=begin
%>
<li>
  <%= link_to user_game_hards_index_path, class: "nav-link text-dark" do %>
    <span>ゲームハード一覧</span>
  <% end %> 
</li>

<li>
  <%= link_to user_genres_index_path, class: "nav-link text-dark" do %>
    <span>ジャンル一覧</span>
  <% end %>
</li>
<%
=end
%>
  <%= link_to destroy_user_session_path, method: :delete, class: "nav-link text-dark" do %>
  <i class="fas fa-sign-out-alt"></i>
    <span>ログアウト</span>
  <% end %>
  <% unless current_user.email == current_user.guest_email %>
  <%= link_to user_user_path(current_user.id), class: "nav-link text-dark" do %>
    <%= image_tag current_user.profile_image, size: '80x80'  %>
    <% end %>
  <% end %>

  </div>

</nav>

<main>